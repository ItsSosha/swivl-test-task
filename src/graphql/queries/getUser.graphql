query GetUser($login: String!) {
  user(login: $login) {
    id
    avatarUrl
    name
    login
    company
    email
    location
    websiteUrl
    createdAt
    bio
    socialAccounts(first: 5) {
      nodes {
        displayName
        provider
        url
      }
    }
  }
}
